from fastapi import APIRouter, Depends\nfrom app.api.deps import get_current_user\n\nrouter = APIRouter()\n\n@router.get("/")\nasync def get_root(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/parse")\nasync def get_parse(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/{reminder_id}/complete")\nasync def get_reminder_id_complete(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n