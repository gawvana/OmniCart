from fastapi import APIRouter, Depends\nfrom app.api.deps import get_current_user\n\nrouter = APIRouter()\n\n@router.get("/{list_id}")\nasync def get_list_id(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/{list_id}/bulk")\nasync def get_list_id_bulk(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/{item_id}")\nasync def get_item_id(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/{item_id}/purchase")\nasync def get_item_id_purchase(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n