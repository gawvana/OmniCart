from fastapi import APIRouter, Depends\nfrom app.api.deps import get_current_user\n\nrouter = APIRouter()\n\n@router.get("/")\nasync def get_root(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/overview")\nasync def get_overview(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/by-category")\nasync def get_by_category(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/by-day")\nasync def get_by_day(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/frequent")\nasync def get_frequent(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n