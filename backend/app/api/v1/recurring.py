from fastapi import APIRouter, Depends\nfrom app.api.deps import get_current_user\n\nrouter = APIRouter()\n\n@router.get("/")\nasync def get_root(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/{item_id}")\nasync def get_item_id(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/due")\nasync def get_due(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/{item_id}/mark-added")\nasync def get_item_id_mark_added(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n