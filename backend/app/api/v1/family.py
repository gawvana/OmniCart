from fastapi import APIRouter, Depends\nfrom app.api.deps import get_current_user\n\nrouter = APIRouter()\n\n@router.get("/")\nasync def get_root(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/{family_id}/members")\nasync def get_family_id_members(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/{family_id}/invite")\nasync def get_family_id_invite(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/join")\nasync def get_join(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/{family_id}/members/{member_id}")\nasync def get_family_id_members_member_id(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/{family_id}/activity")\nasync def get_family_id_activity(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n