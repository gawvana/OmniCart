from fastapi import APIRouter, Depends\nfrom app.api.deps import get_current_user\n\nrouter = APIRouter()\n\n@router.get("/")\nasync def get_root(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/{notification_id}/read")\nasync def get_notification_id_read(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/read-all")\nasync def get_read_all(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n