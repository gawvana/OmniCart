from fastapi import APIRouter, Depends\nfrom app.api.deps import get_current_user\n\nrouter = APIRouter()\n\n@router.get("/parse")\nasync def get_parse(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/plan")\nasync def get_plan(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/categorize")\nasync def get_categorize(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/budget-suggestions")\nasync def get_budget_suggestions(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n@router.get("/insights")\nasync def get_insights(current_user = Depends(get_current_user)):\n    return {"data": "Not implemented"}\n\n